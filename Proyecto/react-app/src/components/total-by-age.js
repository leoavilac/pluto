import PropTypes from 'prop-types'

const TotalByAge = () => {
    return (
        <div className="container">
            <h1><strong>R E P O R T E   # 3</strong></h1>
            <p className="text-muted">Gráfica de barras del rango de edades por cada país</p>
            <hr />
        </div>
    )
}

TotalByAge.defaultProps = {
}

TotalByAge.propTypes = {
}

export default TotalByAge